const initialState = {};

const ActionType = {};

const reducer = (state = initialState, actiob) => {
  // switch (action.type) {
    // case ActionType. :
    //   return
  // }

  return state;
};

export {reducer, ActionType};
